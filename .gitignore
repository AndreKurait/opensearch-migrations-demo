/target
**/*.rs.bk
Cargo.lock.bak

# CI writes a cross-linker config here for the aarch64-linux build.
/.cargo/config.toml

# Generated provisioning workspace (the harness writes plans + manifests here).
/migration-demo-workspace/

# Terraform state (the harness emits .tf; users run it themselves).
**/.terraform/
**/*.tfstate
**/*.tfstate.backup
**/.terraform.lock.hcl

.DS_Store
