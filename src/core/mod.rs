//! Foundation: error type, the external-command seam, small utilities, and the
//! persisted plan state. Each leaf module is re-exported at the crate root.

pub mod error;
pub mod runner;
pub mod state;
pub mod util;
